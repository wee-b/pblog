

project:
  name: pblog-admin
  log-directory: ${localPath:/home}/logs/${project.name}/${spring.profiles.active}


server:
  port: 8081
  servlet:
    context-path: /

# 环境
spring:
  profiles:
    active: dev
  config:
    import: classpath:dev/base.yaml